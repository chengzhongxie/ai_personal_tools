<UserControl x:Class="PersonalAssistant.Features.Chat.Views.ChatView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             xmlns:helpers="clr-namespace:PersonalAssistant.Infrastructure.Common.Helpers"
             mc:Ignorable="d"
             d:DesignHeight="650" d:DesignWidth="900">

    <UserControl.Resources>
        <helpers:BoolToVisibilityConverter x:Key="BoolToVis" />
        <helpers:MessageAlignmentConverter x:Key="MessageAlignment" />
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Border Grid.Row="0" Padding="12,10" Background="#F5F5F5">
            <StackPanel>
                <TextBlock FontSize="15" FontWeight="SemiBold" Text="Personal AI Assistant" />
                <TextBlock Foreground="Gray" FontSize="12" Text="Powered by DeepSeek — Ask me anything, I can read/write files, run commands, and fetch web content." />
            </StackPanel>
        </Border>

        <!-- Messages Area -->
        <ScrollViewer Grid.Row="1" x:Name="MessageScroll"
                      VerticalScrollBarVisibility="Auto"
                      Background="Transparent">
            <ItemsControl ItemsSource="{Binding ViewModel.Messages}"
                          Margin="12,8">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="0,4">
                            <Border Padding="10,8"
                                    CornerRadius="8"
                                    MaxWidth="600">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Role}" Value="User">
                                                <Setter Property="Background" Value="#0078D4" />
                                                <Setter Property="HorizontalAlignment" Value="Right" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Role}" Value="Assistant">
                                                <Setter Property="Background" Value="#F0F0F0" />
                                                <Setter Property="HorizontalAlignment" Value="Left" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Role}" Value="System">
                                                <Setter Property="Background" Value="Transparent" />
                                                <Setter Property="HorizontalAlignment" Value="Center" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Role}" Value="Tool">
                                                <Setter Property="Background" Value="Transparent" />
                                                <Setter Property="HorizontalAlignment" Value="Center" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>

                                <StackPanel>
                                    <TextBlock Text="{Binding Content}"
                                               TextWrapping="Wrap">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Role}" Value="User">
                                                        <Setter Property="Foreground" Value="White" />
                                                        <Setter Property="FontSize" Value="13" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Role}" Value="Assistant">
                                                        <Setter Property="Foreground" Value="#1A1A1A" />
                                                        <Setter Property="FontSize" Value="13" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Role}" Value="System">
                                                        <Setter Property="Foreground" Value="Gray" />
                                                        <Setter Property="FontSize" Value="11" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Role}" Value="Tool">
                                                        <Setter Property="Foreground" Value="Gray" />
                                                        <Setter Property="FontSize" Value="11" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>

                                    <TextBlock Text="{Binding Timestamp, StringFormat='{}{0:HH:mm}'}"
                                               FontSize="10"
                                               Margin="0,4,0,0">
                                        <TextBlock.Style>
                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding Role}" Value="User">
                                                        <Setter Property="Foreground" Value="#B0D4F1" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding Role}" Value="Assistant">
                                                        <Setter Property="Foreground" Value="#999" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </TextBlock.Style>
                                    </TextBlock>
                                </StackPanel>
                            </Border>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>

        <!-- 错误提示条 -->
        <ui:InfoBar Grid.Row="2"
                    Margin="12,4"
                    IsOpen="{Binding ViewModel.ShowInfoBar}"
                    Title="错误"
                    Message="{Binding ViewModel.InfoBarMessage}"
                    Severity="{Binding ViewModel.InfoBarSeverity}"
                    IsClosable="True" />

        <!-- Input Area -->
        <Border Grid.Row="3" Padding="12,10" Background="#F5F5F5">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBox x:Name="InputTextBox"
                         Grid.Column="0"
                         Text="{Binding ViewModel.InputText, UpdateSourceTrigger=PropertyChanged}"
                         VerticalAlignment="Center"
                         Padding="8,6"
                         BorderBrush="#E0E0E0"
                         PreviewKeyDown="InputTextBox_PreviewKeyDown" />

                <ui:ProgressRing Grid.Column="1"
                                 Margin="8,0,0,0"
                                 Width="24" Height="24"
                                 IsIndeterminate="{Binding ViewModel.IsWorking}"
                                 Visibility="{Binding ViewModel.IsWorking, Converter={StaticResource BoolToVis}}" />

                <Button Grid.Column="2"
                        Margin="8,0,0,0"
                        Padding="12,6"
                        Command="{Binding ViewModel.SendCommand}"
                        Background="#0078D4"
                        Foreground="White"
                        BorderThickness="0"
                        VerticalAlignment="Center">
                    <Button.Resources>
                        <Style TargetType="Border">
                            <Setter Property="CornerRadius" Value="4" />
                        </Style>
                    </Button.Resources>
                    Send
                </Button>
            </Grid>
        </Border>
    </Grid>
</UserControl>
