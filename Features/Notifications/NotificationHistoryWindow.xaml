<Window x:Class="PersonalAssistant.Features.Notifications.NotificationHistoryWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="通知历史" Height="400" Width="550"
        Background="#131720"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip">

    <Window.Resources>
        <Style TargetType="ListBox" x:Key="NotificationListStyle">
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Foreground" Value="#D1D5DB" />
        </Style>
    </Window.Resources>

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- 标题 -->
        <TextBlock Grid.Row="0" Text="通知历史"
                   FontSize="18" FontWeight="Bold"
                   Foreground="#E8ECF1" Margin="0,0,0,12" />

        <!-- 通知列表 -->
        <ListBox Grid.Row="1" x:Name="NotificationListBox"
                 Style="{StaticResource NotificationListStyle}"
                 ScrollViewer.VerticalScrollBarVisibility="Auto">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border Padding="12,10" Margin="0,0,0,6"
                            Background="#1A1F2E" CornerRadius="8"
                            BorderBrush="#2A3040" BorderThickness="1">
                        <Grid>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <!-- 标题 + 来源 -->
                            <StackPanel Grid.Row="0" Grid.Column="0" Orientation="Horizontal">
                                <TextBlock Foreground="#E8ECF1" FontSize="13"
                                           FontWeight="SemiBold"
                                           Text="{Binding Title}" />
                                <TextBlock Foreground="#4B5563" FontSize="10"
                                           Margin="8,0,0,0" VerticalAlignment="Center">
                                    <Run Text="[" />
                                    <Run Text="{Binding Source}" />
                                    <Run Text="]" />
                                </TextBlock>
                            </StackPanel>

                            <!-- 时间 -->
                            <TextBlock Grid.Row="0" Grid.Column="1"
                                       Foreground="#6B7280" FontSize="10"
                                       VerticalAlignment="Center"
                                       Text="{Binding TimeDisplay}" />

                            <!-- 内容 -->
                            <TextBlock Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2"
                                       Foreground="#9CA3AF" FontSize="12"
                                       Margin="0,4,0,0"
                                       TextWrapping="Wrap"
                                       Text="{Binding Message}" />
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- 空状态提示 -->
        <TextBlock Grid.Row="1" x:Name="EmptyHint"
                   Text="暂无通知记录"
                   Foreground="#4B5563" FontSize="14"
                   HorizontalAlignment="Center" VerticalAlignment="Center"
                   Visibility="Collapsed" />
    </Grid>
</Window>
